language: cpp
matrix:
  include:
    - os: windows
      script:
        - mkdir build && cd build
        - cmake ..
        - cmake --build .

addons:
  homebrew:
    packages:
    - hdf5
  apt:
    packages:
    - libhdf5-dev


cache:
  directories:
    - $HOME/.cache/pip

# language: python
# python:
#   - 3.7

install: skip
script:
  - python get_hdf5.py
  - mkdir build && cd build
  - cmake ..
  - cmake --build .
