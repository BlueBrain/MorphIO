jobs:
  include:
    - os: linux
      dist: xenial
      env:
        CXX: g++
        CC: gcc
    - os: linux
      dist: xenial
      env:
        CXX: clang++
        CC: clang

    - os: osx
      osx_image: xcode10.1
      language: generic
      env:
        CXX: clang++
        CC: clang
    - os: osx
      osx_image: xcode10.1
      language: generic
      env:
        CXX: g++
        CC: gcc

# cache:
#   directories:
#     - $HOME/.cache/pip
#     - $HOME/Library/Caches/Homebrew
language: python
python:
  - 3.7

before_install:
  - ./ci/before_install.sh
install: skip
script:
  - ./ci/python_test.sh
  - ./ci/cpp_test.sh
