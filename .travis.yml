matrix:
  include:
    # - os: linux
    #   dist: trusty
    #   sudo: required
    - os: osx
      osx_image: xcode10.1
cache:
  directories:
    - $HOME/.cache/pip
    - $HOME/Library/Caches/Homebrew
language: c++
sudo: required
services:
  - docker
python:
  # - 2.7
  # - 3.5
  - 3.6
# addons:
#   apt:
#     packages:
#       - libhdf5-dev
#       - g++-5
#     sources: &sources
#       - llvm-toolchain-precise-3.8
#       - ubuntu-toolchain-r-test
before_install:
  - if [ $TRAVIS_OS_NAME = linux ]; then sudo apt-get install libhdf5-dev; else brew install hdf5; fi
  - pip install virtualenv
  - virtualenv env && source env/bin/activate
install:
  - pip install --upgrade pip
  - pip install .
script:
  - ./scripts/check-clang-format-all
  - pip install -r tests/requirement_tests.txt
  - nosetests

# # before_deploy:
#   - pip install delocate
#   # Build macos wheel
#   - ./packaging/python_wheel/build_wheel.sh

#   # Build Linux wheels with the manylinux docker image
#   - ./packaging/python_wheel/build_docker_image.sh
#   - ./packaging/python_wheel/build_wheel_in_manylinux1_docker.sh

deploy:
  provider: pypi
  user: bbp.opensource
  password:
    secure: N/2AGpTUSOGOxQqYulja5Etj228w1SByVuX+ozPvuPJTm5kJh5fqav8FVtPLziaWAaWxJ6W2nFdWT9AwodwMk3+1NBHMWpOZ3NJnvibz0cvYCkXdNcgXOFGrPSeX3Oo2d74IeT20R44lieZEW62j4488SSaVkwr3p7YgyfVMVGo=
  on:
    tags: true
    branch: master
